# Аудит текстов вкладок и документов от 2026-06-27

## Проверено

- Заголовки вкладок и секций `MainWindow.xaml`.
- Подписи полей вкладок: трансформатор/корпус, РУВН, РУНН, собственные нужды, результат, документы.
- Строки паспорта металлоконструкции, приказа, чек-листа ОТК и спецификации.
- Предупреждения/сводки результата расчета.
- Автопрофили сетевых компаний и старые поля, которые могли снова проявиться в интерфейсе.

## Исправлено

- Убраны из рабочих профилей и модели старые поля `кабельные вводы`, `фиксаторы дверей`, старый общий `тип замка`.
- Убраны неиспользуемые UI-источники старого выбора разрядного тока ОПН; видимый выбор ОПН остается по пропускной способности.
- Вкладка корпуса:
  - `Трансф. отсек` заменено на `Трансформаторный отсек`;
  - `Паспортная/предупреждающая табличка` заменено на `Паспортная / предупреждающая табличка`;
  - блок шин переименован в `Шины РУВН, РУНН и N/PE`.
- Вкладка РУВН:
  - `Опер. питание` заменено на `Оперативное питание`;
  - `Рекоменд. Uнр` заменено на `Рекомендуемое Uнр`.
- Вкладка собственных нужд:
  - `Кол-во светильников` заменено на `Количество светильников`;
  - `Резервный источник снабжения электроэнергией` заменено на `Резервный источник электроснабжения`.
- Вкладка результата:
  - `Цвета и корпус` заменено на `RAL и корпус`.
- Вкладка документов:
  - `Чек-лист ОТК (Превью)` заменено на `Чек-лист ОТК`;
  - `Настройка шаблона Чек-листа` заменено на `Настройка шаблона чек-листа`;
  - пояснение про вкладку превью заменено на нейтральное пояснение про вкладку чек-листа.
- Паспорт и приказ:
  - `Цветовые зоны` заменено на `Окраска по зонам`;
  - `Итоговая Масса Брутто` заменено на `Итоговая масса брутто`;
  - `Сети / типовой проект` заменено на `Сетевая компания / типоразмер`;
  - `Металл корпуса` заменено на `Материал корпуса`;
  - `Прим.` заменено на `Примечание`;
  - `паспортная/предупреждающая табличка` заменено на `паспортная / предупреждающая табличка`.
- Спецификация:
  - `Кол-во` заменено на `Количество`.
- Чек-лист ОТК:
  - исправлены грамматические ошибки `фактическое оборудования`;
  - `630 кВ` исправлено на `630 кВА`;
  - `на крыши` исправлено на `на крыше`;
  - `Над двери` исправлено на `Над дверями`;
  - `220В` исправлено на `220 В`;
  - `РУ-6 кВ/10кВ` исправлено на `РУ-6/10 кВ`;
  - `Стой напряжение` исправлено на `Стой! Напряжение`;
  - старый пункт про болт заземления заменен на `Точка подключения заземления`;
  - `Установить стопора-фиксаторы ТМГ` заменено на `Проверить фиксаторы дверей ТМГ`;
  - `Вкл/выкл рычаги` заменено на `Проверить включение/отключение рычагов`;
  - исправлены несколько технологических формулировок во внутреннем монтаже.

## Что оставлено специально

- Внутренняя миграция старых проектов продолжает распознавать старые значения вроде `Болт заземления на корпусе`, чтобы при открытии старого файла заменить их на актуальные значения. В интерфейс и документы эти старые строки не выводятся.
- Вкладку `Схема` не развивал и не менял по логике, потому что схемы сейчас не в работе.
- Исторические документы в `план развития` не чистил полностью: они отражают старые этапы и могут пригодиться как история решений.

## Проверка

- `dotnet test KtpnConfigurator.slnx -c Release` - 72/72.
- `build/KtpnConfigurator_V6/Конфигуратор КТПН.exe --selftest` - успешно.
- Основной exe обновлен: `build/KtpnConfigurator_V6/Конфигуратор КТПН.exe`.
