# Восстановление работы над KtpnConfigurator

Последнее обновление: 15.07.2026

Этот файл нужно читать первым после перезапуска Codex или переноса проекта.
Он содержит текущее состояние, важные запреты, рабочие пути и порядок продолжения.
Полная хронология решений хранится в `CODEX_SESSION_STATE.md`.

## 1. Где продолжается разработка

Активная версия:

`versions/KtpnConfigurator_Optimized`

Рабочие исходники:

- `versions/KtpnConfigurator_Optimized/src`
- `versions/KtpnConfigurator_Optimized/tests`

Готовое приложение:

`versions/KtpnConfigurator_Optimized/build/KtpnConfigurator_Optimized/Конфигуратор КТПН Optimized.exe`

Важно: папка `build/` не хранится в git (`.gitignore`). После клонирования
репозитория exe по этому пути нет — его нужно собрать из папки
`versions/KtpnConfigurator_Optimized` командой `build.ps1` (см. раздел 9).

Корневая версия `src`, `tests` и прежняя V6 являются исходной базой и историей.
Новые функциональные изменения выполнять только в Optimized, пока пользователь явно не решит иначе.

## 2. Что прочитать для полного восстановления

1. `CODEX_RECOVERY_GUIDE.md` - текущее краткое состояние.
2. `CODEX_SESSION_STATE.md` - подробная хронология решений пользователя и выполненных изменений.
3. `versions/KtpnConfigurator_Optimized/OPTIMIZED_SESSION_STATE.md` - история активной версии.
4. `план развития/CODEX_АРХИТЕКТУРА_2КТПН_ЩО_КСО_ВРУ_НКУ_КРУ_2026-07-04.md` - архитектура новых изделий.
5. Профильный аудит из `план развития`, если работа касается конкретной вкладки или документа.

## 3. Неподвижные решения пользователя

- Работа ведется через ИИ-агентов (исторически Codex, с 07.2026 также Claude Code); журналы восстановления обязательны для любого агента.
- Схемы пока не развивать и не включать. Их код не удалять без отдельного решения.
- Не углубляться в полноценную селективность.
- Текущее простое правило: номинал отходящего автомата не выше ограничивающего вводного аппарата.
- ТТ подбираются по номиналу соответствующего вводного автомата или ограничивающего аппарата.
- Не выдумывать характеристики оборудования. Неподтвержденные параметры помечать `Требуется проверка`.
- Фамилии не должны появляться в документах.
- Бренд отходящей линии сохраняется в строке конкретной линии.
- Заказчик выбирается один раз в шапке; его профиль молча настраивает проект. Не повторять название заказчика и ссылки на его ТЗ в каждом разделе.
- Ошибки допускается принять для передачи проекта в работу через явное согласие пользователя.
- При отсутствии ошибок и предупреждений не показывать нулевые счетчики.
- Предупреждение о неназначенной отходящей линии РУНН не требуется.
- При выборе NH рядом показывается только надпись `РИСЭ`, пока РИСЭ не включена.
- Чек-лист: A4 книжный, без комментариев и без фоновой заливки.

## 4. Текущее состояние Optimized

- WPF, .NET 10.
- Основные вкладки: проект, трансформатор и корпус, РУВН, РУНН, собственные нужды, результат, документы.
- Схемы отключены в UI и экспорте.
- РУНН использует строки отходящих линий с номером, типом, брендом, моделью, номиналом и учетом.
- Автоподбор ввода, ТТ и шин исправлен и разделен по назначению оборудования.
- Шины РУВН и РУНН рассчитываются по току выбранного трансформатора и выбранному материалу.
- Документы: приказ, паспорт, паспорт металлоконструкции, спецификация и чек-лист.
- Сохранение атомарное, имеются резервная копия, автосохранение и архив проекта.
- Индикаторы вкладок: `Заполнено`, `Требуется проверка`, `Есть ошибка`.
- Последний подтвержденный прогон до подготовки новых изделий: 111/111 тестов.
- После исправления выбора Т1/Т2 и редакторов линеек: 129/129 тестов, selftest всех семи типов и UI smoke-test успешны.
- 11.07.2026 исправлены вылеты при смене изделия (этапы 0-1, `Stage01RegressionTests`).
- 15.07.2026 (этап 2, `Stage2LineupPreservationTests`): линейки панелей/ячеек сохраняются по изделиям и не перезаписываются шаблоном при переключении; пересчет защищен от повторного входа; null от WPF-привязок не затирает значения; скрытая вкладка автоматически заменяется видимой (и при смене изделия, и при открытии файла другого изделия). Прогон: 169/169 тестов, selftest и UI smoke-test успешны; живая UI-проверка через UI Automation подтвердила сохранность линеек и переключения за 4-136 мс без зависаний.

## 5. Новые изделия от 04.07.2026

Текущий формат проекта - версия 9 (`ProjectConfig.CurrentVersion`); 04.07 формат
был поднят до 5, затем развитие изделий довело его до 8, а 15.07 добавлены
раздельные линейки по изделиям (версия 9).

В `ProjectConfig` добавлены:

- `ProductTypeId` - устойчивый идентификатор изделия;
- `ProductDataVersion` - версия структуры данных конкретного изделия;
- `LowVoltageAssemblyByProduct` / `MediumVoltageSwitchgearByProduct` - линейки
  панелей/ячеек, настроенные для других изделий семейства (активная линейка
  относится к текущему `ProductTypeId` и подменяется при смене изделия).

В `ProductArchitecture.cs` создан реестр:

- `ktpn.single` - КТПН;
- `ktpn.double` - 2КТПН;
- `nku.general` - НКУ;
- `nku.shcho` - ЩО;
- `nku.vru` - ВРУ;
- `mv.kso` - КСО;
- `mv.kru` - КРУ.

Все изделия доступны в поле `Изделие` на вкладке `Проект`.
Старые `.ktpn` автоматически мигрируют в `ktpn.single`.
Подробности реализации: `план развития/CODEX_ВНЕДРЕНИЕ_НОВЫХ_ИЗДЕЛИЙ_2026-07-04.md`.

## 6. Архитектурное направление

Не создавать шесть отдельных приложений и не добавлять шесть названий к существующим вкладкам КТПН.

- КТПН и 2КТПН - семейство трансформаторных подстанций.
- НКУ - общее ядро низковольтных устройств.
- ЩО и ВРУ - профили ядра НКУ.
- КСО и КРУ - общее ядро линейки ячеек среднего напряжения.
- КРУ расширяет это ядро отсеками, IAC/LSC, РЗА и сложными блокировками.

Рекомендуемый порядок:

1. Основа модулей изделий.
2. 2КТПН.
3. НКУ и ЩО.
4. ВРУ.
5. КСО.
6. КРУ.

## 7. Следующий инженерный этап

Новые изделия уже работают. Для углубления базы и расчетов потребуется собрать:

- могут ли Т1 и Т2 иметь разные мощность, марку и производителя;
- допускается ли параллельная работа трансформаторов;
- нормальное положение секционного аппарата;
- какие серии вводных и секционного аппаратов применяются;
- варианты РУВН для двух трансформаторных ветвей;
- типовые компоновки корпуса.

2КТПН уже содержит:

- Т1 и Т2;
- секции РУНН 1 и 2;
- два вводных аппарата;
- секционный аппарат;
- АВР или ручное секционирование;
- принадлежность каждой линии своей секции;
- проверку нормального и аварийного режима;
- запрет недопустимой параллельной работы.

## 8. Структура папки после очистки

- `.git` - история Git, не удалять.
- `versions/KtpnConfigurator_Optimized` - активная версия.
- `план развития/Примеры` - исходные требования и материалы заказчиков, не удалять.
- `reference_materials` - эталонные документы и чертежи, не редактировать.
- `_oracle` - эталонные таблицы и golden-данные тестов.
- `src`, `tests`, `docs` - корневая базовая версия и документация.
- `план развития` - технические аудиты и принятые решения.

04.07.2026 удалены только воспроизводимые и лишние данные:

- старая корневая папка `build`;
- временные `bin/obj` Optimized;
- два посторонних prompt-файла о спортивных ставках;
- устаревший рабочий prompt, противоречащий текущей паузе по схемам.

Рабочая опубликованная сборка Optimized сохранена.
Контрольная копия прежней V6 также существует вне очищаемой папки:
`C:\Users\Simps\Desktop\KtpnConfigurator\build\KtpnConfigurator_V6\Конфигуратор КТПН.exe`.
Ее размер `72 935 717` байт, SHA-256 `1E7A638EA470B90333C80EDB1F0A2532702860CBEDC37BE70F69FABD21046664`.

## 9. Проверка и сборка

Из папки `versions/KtpnConfigurator_Optimized`:

```powershell
dotnet test KtpnConfigurator.slnx -c Release
powershell -ExecutionPolicy Bypass -File .\build.ps1
```

После публикации проверить:

```powershell
.\build\KtpnConfigurator_Optimized\Конфигуратор КТПН Optimized.exe --selftest
.\build\KtpnConfigurator_Optimized\Конфигуратор КТПН Optimized.exe --smoketest-ui
```

После проверки временные `bin/obj` можно удалить, опубликованный `build` оставить.

Текущая сборка от 15.07.2026:

- размер: `73 087 339` байт;
- SHA-256: `F5833F3A6BE3715DC50E73CC173F5757AB9E1CBE08F50A6D6CF0DD73FAC6B258`.

Числа тестов в датированных блоках журналов историчны (67 -> 72 -> ... -> 129);
актуальный прогон 15.07.2026 - 169/169.

## 10. Как вести журнал дальше

После каждого завершенного этапа:

1. Обновить этот файл, если изменились активная версия, запреты или следующий этап.
2. Дописать датированный блок в `CODEX_SESSION_STATE.md`.
3. Дописать краткий блок в `versions/KtpnConfigurator_Optimized/OPTIMIZED_SESSION_STATE.md`.
4. Указать измененные файлы, смысл решений, результаты тестов и путь к exe.
5. Если создается большой аудит, сохранить его в `план развития` и дать ссылку из журнала.
